#!/bin/sh

# Single sing on variables
export SSO_HOST=localhost
export SSO_PORT=10100
export SSO_URL_BASE="http://${SSO_HOST}:${SSO_PORT}/v1/sso"

# Adding the bats interpreter to the PATH
export PATH=$PATH:"$BATS_HOME/bin"
