#!/bin/sh

# Single sing on variables
export SSO_HOST=lola.tq.uki
export SSO_PORT=80
export RPROXY_PREFIX="/api/auth"
export SSO_URL_BASE="http://${SSO_HOST}:${SSO_PORT}/${RPROXY_PREFIX}/v1/sso"

# Adding the bats interpreter to the PATH
export PATH=$PATH:"$BATS_HOME/bin"
